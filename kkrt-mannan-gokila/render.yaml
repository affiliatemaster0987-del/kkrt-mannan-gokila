services:
  - type: web
    name: krt-ai-terminal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TG_TOKEN
        sync: false
      - key: TG_CHAT
        sync: false
